<!-- ======================================================================= -->
<!-- $Id: web-tests.xml 1694 2006-07-09 05:22:30Z mraible $               -->
<!-- ======================================================================= -->
<!DOCTYPE project [
    <!ENTITY config SYSTEM "file:./config.xml">
    <!ENTITY login SYSTEM "file:./login.xml">
]>
<project basedir="." default="run-all-tests"> 
    <!-- Use i18n bundle, 2nd one listed is the default -->
    <property file="${build.dir}/test/properties/ApplicationResources_${user.language}.properties"/>
    <property file="web/WEB-INF/classes/ApplicationResources.properties"/>

    <!-- runs all targets -->
    <target name="run-all-tests" 
        depends="Login,Logout,PasswordHint,Signup,UserTests,FlushCache,FileUpload,LookupDefItemTests,LookupDefTests,SettingTests,AccountTests,AccountTests,AccountTests"
        description="Call and executes all test cases (targets)"/>
    
    <!-- runs user-related tests -->
    <target name="UserTests" 
        depends="EditUser,SearchUser,SaveUser,AddUser,ActiveUsers"
        description="Call and executes all user test cases (targets)">
        <echo>Successfully ran all User JSP tests!</echo>
    </target>
    
    <!-- Login to the application -->
    <target name="Login" 
        description="Runs login test and verifies Main Menu's Title">
        <webtest name="login">
            &config;
            <steps>
                &login;
            </steps>
        </webtest>
    </target>

    <!-- Logout of the application -->
    <target name="Logout"
        description="Runs logout test and verifies Login's Title">
        <webtest name="login">
            &config;
            <steps>
                &login;
                <invoke description="get Logout Page" url="/logout.jsp"/>
                <verifytitle description="we should see the login title"
                    text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Retrieve Password Hint -->
    <target name="PasswordHint" 
        description="Gets the user's password hint">
        <webtest name="passwordHint">
            &config;
            <steps>
                <invoke description="get Login Page" url="/"/>
                <verifytitle description="we should see the login title" 
                    text=".*${login.title}.*" regex="true"/>
                <invoke description="click password hint link"
                    url="/passwordHint.html?username=mraible"/>
                <verifytitle description="we should see the login title again"
                    text=".*${login.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Verify the edit user screen displays without errors -->
    <target name="EditUser" 
        description="Tests selecting the 'Edit Profile' forward">
        <webtest name="editUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/editProfile.html"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Verify the user list screen displays without errors -->
    <target name="SearchUser" 
        description="Tests search for and displaying all users">
        <webtest name="searchUser">
            &config;
            <steps>
                &login;
                <invoke description="click View Users link" url="/users.html"/>
                <verifytitle description="we should see the user list title" 
                    text=".*${userList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Edit a user and then save -->
    <target name="SaveUser" 
        description="Tests editing and saving a user">
        <webtest name="saveUser">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Profile link" url="/editProfile.html"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
                <setinputfield description="set address" name="address.address" value="2255 Lane Avenue"/>
                <setinputfield description="set zipcode" name="address.postalCode" value="59826"/>
                <clickbutton label="${button.save}" description="Click Save"/>
                <verifytitle description="display Main Menu if save OK"
                     text=".*${mainMenu.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Add a new user after logging in -->
    <target name="AddUser"
        description="Adds a new user profile">
        <webtest name="addUser">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" 
                    url="/editUser.html?method=Add&amp;from=list"/>
                <verifytitle description="we should see the user profile title" 
                    text=".*${userProfile.title}.*" regex="true"/>
                    
                <setinputfield description="set username" name="username" value="newuser"/>
                <setinputfield description="set password" name="password" value="test"/>
                <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/>
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="address.city" value="Denver"/>
                <setinputfield description="select province" name="address.province" value="CO"/>
                <setselectfield description="select country" name="address.country" value="US"/>
                <setinputfield description="set postalCode" name="address.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="newuser@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <setselectfield description="select roles" name="userRoles" value="user"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>
                
                <verifytitle description="Redisplay add user page" text=".*${userProfile.title}.*" regex="true"/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="message.*&gt;.*&lt;strong&gt;Test Name&lt;/strong&gt;.*&lt;/div&gt;'/>
                    
                <!-- Delete user -->
                <invoke description="Click edit user link" url="/editUser.html?username=newuser&amp;from=list"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytext description="verify success message" regex="true"
                    text='&lt;div class="message.*&gt;.*&lt;strong&gt;Test Name&lt;/strong&gt;.*&lt;/div&gt;'/>
                <verifytitle description="display user list" text=".*${userList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Sign up a new user (no login) -->
    <target name="Signup" description="Signup a new user">
        <webtest name="signup">
            &config;
            <steps>
                <!-- Logout in case user is already signed in -->
                <invoke description="get Logout Page" url="/logout.jsp"/>

                <invoke description="click signup link" url="/signup.html"/>
                <verifytitle description="view signup title" text=".*${signup.title}.*" regex="true"/>
                
                <setinputfield description="set username" name="username" value="signup-user"/>
                <setinputfield description="set password" name="password" value="test"/>
                <setinputfield description="set confirmPassword" name="confirmPassword" value="test"/>
                <setinputfield description="set firstName" name="firstName" value="Test"/>
                <setinputfield description="set lastName" name="lastName" value="Name"/>
                <setinputfield description="set city" name="address.city" value="Denver"/>
                <setinputfield description="set state" name="address.province" value="CO"/>
                <setselectfield description="select country" name="address.country" value="US"/>
                <setinputfield description="set postalCode" name="address.postalCode" value="80210"/>
                <setinputfield description="set email" name="email" value="signup-user@raibledesigns.com"/>
                <setinputfield description="set website" name="website" value="http://raibledesigns.com"/>
                <setinputfield description="set passwordHint" name="passwordHint" value="test"/>
                <clickbutton label="${button.register}" description="Click button 'Signup'"/>

                <verifytitle description="view main menu" text=".*${mainMenu.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${user.registered}"/>
            </steps>
        </webtest>
    </target>

    <!-- View Active Users -->
    <target name="ActiveUsers" 
        description="View Active Users List">
        <webtest name="activeUsers">
            &config;
            <steps>
                &login;
                <invoke description="get activeUsers URL" url="/activeUsers.html"/>
                <verifytitle description="we should see the activeUsers title" 
                    text=".*${activeUsers.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- Flush OSCache -->
    <target name="FlushCache" 
        description="opens JSP to Flush Page Cache">
        <webtest name="flushCache">
            &config;
            <steps>
                &login;
                <invoke description="get flushCache URL" url="/flushCache.html"/>
                <verifytitle description="we should see the flush cache title" 
                    text=".*${flushCache.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
    
    <!-- File Upload -->
    <target name="FileUpload" 
        description="Tests uploading a file">
        <webtest name="fileUpload">
            &config;
            <steps>
                &login;
                <invoke description="click Upload a File link" url="/selectFile.html"/>
                <verifytitle description="we should see file upload form" 
                    text=".*${upload.title}.*" regex="true"/>
                <!--setinputfield description="set name" name="name" 
                    value="Canoo Test File"/>
                <setinputfield description="set file" name="file" 
                    value="${basedir}/build.properties"/>
                <clickbutton label="${button.upload}" description="Click Upload"/>
                <verifytitle description="show file data if upload OK"
                     text=".*${display.title}.*" regex="true"/>
                <verifytext description="verify original file name displayed on page"
                     text="build.properties" /-->
            </steps>
        </webtest>
    </target>





<!--Account-START-->
    <!-- runs account-related tests -->
    <target name="AccountTests"
        depends="SearchAccounts,EditAccount,SaveAccount,AddAccount,DeleteAccount"
        description="Call and executes all account test cases (targets)">
        <echo>Successfully ran all Account JSP tests!</echo>
    </target>

    <!-- Verify the edit account screen displays without errors -->
    <target name="EditAccount"
        description="Tests editing an existing Account's information">
        <webtest name="editAccount">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Account link" url="/editAccount.html?id=1"/>
                <verifytitle description="we should see the accountDetail title"
                    text=".*${accountDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a account and then save -->
    <target name="SaveAccount"
        description="Tests editing and saving a account">
        <webtest name="saveAccount">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Account link" url="/editAccount.html?id=1"/>
                <verifytitle description="we should see the accountDetail title"
                    text=".*${accountDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set companyName" name="companyName" value="companyName"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${accountDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Account -->
    <target name="AddAccount"
        description="Adds a new Account">
        <webtest name="addAccount">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editAccount.html"/>
                <verifytitle description="we should see the accountDetail title"
                    text=".*${accountDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set companyName" name="companyName" value="JhDbMiUvMoMhZsXtJeTsFwKcMsXoGtRvPpEdRdHtKfSjXrSwAe"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Account List appears if save successful"
                    text=".*${accountList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${account.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing account -->
    <target name="DeleteAccount"
        description="Deletes existing Account">
        <webtest name="deleteAccount">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Account link" url="/editAccount.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Account List" text=".*${accountList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${account.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchAccounts"
        description="Tests search for and displaying all accounts">
        <webtest name="searchAccounts">
            &config;
            <steps>
                &login;
                <invoke description="click View Accounts link" url="/accounts.html"/>
                <verifytitle description="we should see the accountList title"
                    text=".*${accountList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Account-END-->

<!--Setting-START-->
    <!-- runs setting-related tests -->
    <target name="SettingTests"
        depends="SearchSettings,EditSetting,SaveSetting,AddSetting,DeleteSetting"
        description="Call and executes all setting test cases (targets)">
        <echo>Successfully ran all Setting JSP tests!</echo>
    </target>

    <!-- Verify the edit setting screen displays without errors -->
    <target name="EditSetting"
        description="Tests editing an existing Setting's information">
        <webtest name="editSetting">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Setting link" url="/editSetting.html?id=1"/>
                <verifytitle description="we should see the settingDetail title"
                    text=".*${settingDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a setting and then save -->
    <target name="SaveSetting"
        description="Tests editing and saving a setting">
        <webtest name="saveSetting">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Setting link" url="/editSetting.html?id=1"/>
                <verifytitle description="we should see the settingDetail title"
                    text=".*${settingDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set settingDescription" name="settingDescription" value="settingDescription"/>
                <setinputfield description="set settingName" name="settingName" value="settingName"/>
                <setinputfield description="set settingValue" name="settingValue" value="settingValue"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${settingDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new Setting -->
    <target name="AddSetting"
        description="Adds a new Setting">
        <webtest name="addSetting">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editSetting.html"/>
                <verifytitle description="we should see the settingDetail title"
                    text=".*${settingDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set settingDescription" name="settingDescription" value="PeOmCwJmLrEuZbRzJkExKdYiWgTlAmRtGoJkMzSbZdTtTiGuCfBnWwRjNlXeIfSaDmWmXvRaEbFkChJaWhUbEqRnMrEvPjUhYlUmOfOqJmKnMuAzWiEjVtDpMoGsCrXxUmUwXuUaKmKhIeUeHzXsQcPnJrOxVaGiElGqHtOeXbIlOeEpDuFmTpQgCmRjVnRdQeOrZeYiPlGzKrQfNbLuMyLkNqUqKhZgRnKyGcEjVnMoGzObYfGnCmSqSxPoZwU"/>
                <setinputfield description="set settingName" name="settingName" value="OpZlBaFfWmSwRdZsDdLsTuMtBmQiOv"/>
                <setinputfield description="set settingValue" name="settingValue" value="ViIyBcMlVjUgUpTdGtXxKyYcSaSbHaQfIxObRtJvSoBrHbOeKdVhJtZnXrSdMhJyIeWvSeBaQnBcWcBaSeFzNqDhYpJcSwFwSyHtFaRqYgHhGmFnEkIeJfFjAkMjLwFuZdTfHwDzGxNrXmEiMeVdWnDbBaClUzPrZzEdTeDvRtCqSwXhOnHtBwUgLfLpJeCaUpTqSuRsCoLfYfSyBgAuUuXmZnCgOkDnFpIgNqNmXaWwPdGhMiInAjIyFnImNrK"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="Setting List appears if save successful"
                    text=".*${settingList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${setting.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing setting -->
    <target name="DeleteSetting"
        description="Deletes existing Setting">
        <webtest name="deleteSetting">
            &config;
            <steps>
                &login;
                <invoke description="click Edit Setting link" url="/editSetting.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display Setting List" text=".*${settingList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${setting.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchSettings"
        description="Tests search for and displaying all settings">
        <webtest name="searchSettings">
            &config;
            <steps>
                &login;
                <invoke description="click View Settings link" url="/settings.html"/>
                <verifytitle description="we should see the settingList title"
                    text=".*${settingList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--Setting-END-->

<!--LookupDef-START-->
    <!-- runs lookupDef-related tests -->
    <target name="LookupDefTests"
        depends="SearchLookupDefs,EditLookupDef,SaveLookupDef,AddLookupDef,DeleteLookupDef"
        description="Call and executes all lookupDef test cases (targets)">
        <echo>Successfully ran all LookupDef JSP tests!</echo>
    </target>

    <!-- Verify the edit lookupDef screen displays without errors -->
    <target name="EditLookupDef"
        description="Tests editing an existing LookupDef's information">
        <webtest name="editLookupDef">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDef link" url="/editLookupDef.html?id=1"/>
                <verifytitle description="we should see the lookupDefDetail title"
                    text=".*${lookupDefDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a lookupDef and then save -->
    <target name="SaveLookupDef"
        description="Tests editing and saving a lookupDef">
        <webtest name="saveLookupDef">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDef link" url="/editLookupDef.html?id=1"/>
                <verifytitle description="we should see the lookupDefDetail title"
                    text=".*${lookupDefDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <setinputfield description="set name" name="name" value="name"/>
                <setinputfield description="set desc" name="desc" value="desc"/>
                <setinputfield description="set lookupDesc1" name="lookupDesc1" value="lookupDesc1"/>
                <setinputfield description="set lookupDesc2" name="lookupDesc2" value="lookupDesc2"/>
                <setinputfield description="set lookupDesc3" name="lookupDesc3" value="lookupDesc3"/>
                <setinputfield description="set lookupDesc4" name="lookupDesc4" value="lookupDesc4"/>
                <setinputfield description="set lookupDesc5" name="lookupDesc5" value="lookupDesc5"/>
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${lookupDefDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new LookupDef -->
    <target name="AddLookupDef"
        description="Adds a new LookupDef">
        <webtest name="addLookupDef">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editLookupDef.html"/>
                <verifytitle description="we should see the lookupDefDetail title"
                    text=".*${lookupDefDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <setinputfield description="set name" name="name" value="MeJaKgBgJuUeCsNsXgGaNyKuKzKzZw"/>
                <setinputfield description="set desc" name="desc" value="GnOjSoHxXdNwSoAeTdWdTeMcMvNaWjAxXrPlYnKzJhAhErRiWxIcAtNaGcTzPxAdDvCdIbJcGqGyMkAuFmTgUnHzSzDwPlIhVuAsLyAqNyAnRcGgMnMkTbOsQvYmLxBzXdNtQuMbUwOjQpFzNxWlVsVvPqOzBuCrXwCxHtKaVhCsImUhKaAuXuHxEfKtRlEbLgUhZyJeUdIcOrYnMyXjQpSlThFoBuHjEtAoDsToEbKePcKdFjMeTjYtCzNqXaL"/>
                <setinputfield description="set lookupDesc1" name="lookupDesc1" value="IuMcVoOpEhTbQmYjYmNb"/>
                <setinputfield description="set lookupDesc2" name="lookupDesc2" value="FjYmZoUpPpHmXpTnJuXu"/>
                <setinputfield description="set lookupDesc3" name="lookupDesc3" value="UbCxJhQrZaEcHoVjSgHf"/>
                <setinputfield description="set lookupDesc4" name="lookupDesc4" value="AfJhTwUeYaOtXtAgWoRw"/>
                <setinputfield description="set lookupDesc5" name="lookupDesc5" value="ChIcMmThMuQaPrBgNzXa"/>
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="LookupDef List appears if save successful"
                    text=".*${lookupDefList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${lookupDef.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing lookupDef -->
    <target name="DeleteLookupDef"
        description="Deletes existing LookupDef">
        <webtest name="deleteLookupDef">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDef link" url="/editLookupDef.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display LookupDef List" text=".*${lookupDefList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${lookupDef.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchLookupDefs"
        description="Tests search for and displaying all lookupDefs">
        <webtest name="searchLookupDefs">
            &config;
            <steps>
                &login;
                <invoke description="click View LookupDefs link" url="/lookupDefs.html"/>
                <verifytitle description="we should see the lookupDefList title"
                    text=".*${lookupDefList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--LookupDef-END-->

<!--LookupDefItem-START-->
    <!-- runs lookupDefItem-related tests -->
    <target name="LookupDefItemTests"
        depends="SearchLookupDefItems,EditLookupDefItem,SaveLookupDefItem,AddLookupDefItem,DeleteLookupDefItem"
        description="Call and executes all lookupDefItem test cases (targets)">
        <echo>Successfully ran all LookupDefItem JSP tests!</echo>
    </target>

    <!-- Verify the edit lookupDefItem screen displays without errors -->
    <target name="EditLookupDefItem"
        description="Tests editing an existing LookupDefItem's information">
        <webtest name="editLookupDefItem">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDefItem link" url="/editLookupDefItem.html?id=1"/>
                <verifytitle description="we should see the lookupDefItemDetail title"
                    text=".*${lookupDefItemDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Edit a lookupDefItem and then save -->
    <target name="SaveLookupDefItem"
        description="Tests editing and saving a lookupDefItem">
        <webtest name="saveLookupDefItem">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDefItem link" url="/editLookupDefItem.html?id=1"/>
                <verifytitle description="we should see the lookupDefItemDetail title"
                    text=".*${lookupDefItemDetail.title}.*" regex="true"/>

                <!-- update some of the required fields -->
                <clickbutton label="${button.save}" description="Click Save"/>

                <verifytitle description="Page re-appears if save successful"
                    text=".*${lookupDefItemDetail.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>

    <!-- Add a new LookupDefItem -->
    <target name="AddLookupDefItem"
        description="Adds a new LookupDefItem">
        <webtest name="addLookupDefItem">
            &config;
            <steps>
                &login;
                <invoke description="click Add Button" url="/editLookupDefItem.html"/>
                <verifytitle description="we should see the lookupDefItemDetail title"
                    text=".*${lookupDefItemDetail.title}.*" regex="true"/>

                <!-- enter required fields -->
                <clickbutton label="${button.save}" description="Click button 'Save'"/>

                <verifytitle description="LookupDefItem List appears if save successful"
                    text=".*${lookupDefItemList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${lookupDefItem.added}"/>
            </steps>
        </webtest>
    </target>

    <!-- Delete existing lookupDefItem -->
    <target name="DeleteLookupDefItem"
        description="Deletes existing LookupDefItem">
        <webtest name="deleteLookupDefItem">
            &config;
            <steps>
                &login;
                <invoke description="click Edit LookupDefItem link" url="/editLookupDefItem.html?id=1"/>
                <prepareDialogResponse description="Confirm delete" dialogType="confirm" response="true"/>
                <clickbutton label="${button.delete}" description="Click button 'Delete'"/>
                <verifyNoDialogResponses/>
                <verifytitle description="display LookupDefItem List" text=".*${lookupDefItemList.title}.*" regex="true"/>
                <verifytext description="verify success message" text="${lookupDefItem.deleted}"/>
            </steps>
        </webtest>
    </target>

    <!-- Verify the people list screen displays without errors -->
    <target name="SearchLookupDefItems"
        description="Tests search for and displaying all lookupDefItems">
        <webtest name="searchLookupDefItems">
            &config;
            <steps>
                &login;
                <invoke description="click View LookupDefItems link" url="/lookupDefItems.html"/>
                <verifytitle description="we should see the lookupDefItemList title"
                    text=".*${lookupDefItemList.title}.*" regex="true"/>
            </steps>
        </webtest>
    </target>
<!--LookupDefItem-END-->
</project>






