<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-mapping PUBLIC
    "-//Hibernate/Hibernate Mapping DTD 3.0//EN" 
    "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>

 
    <sql-query name="findSurveysWithResponseCount">
 		<return alias="survey" class="uk.co.bluetrail.mobriz.model.Survey"/>
	<![CDATA[
	    select s.*, count as responseCount from mr.surveys s, mr.responsecount r where s.id = r.id and s.account_id= :account_id]]>
	</sql-query>


    <sql-query name="findMonthTotals">

        <![CDATA[
	 SELECT (vwtotals.pr_fn || ' ') || vwtotals.pr_ln AS prac_name, sum(vwtotals.num) AS count
   FROM vwtotals
  WHERE date_part('month', vwtotals.updated_at) = :month_selected AND
  date_part('year', vwtotals.updated_at) = :year_selected  and department = :dept_selected
  GROUP BY (vwtotals.pr_fn || ' ') || vwtotals.pr_ln
  ORDER BY (vwtotals.pr_fn || ' ') || vwtotals.pr_ln

]]>

    </sql-query>


    <sql-query name="findMonthDetails">

        <![CDATA[
	 SELECT u.id, vp.projecttitle, vp.prac_first_name AS pracfname, vp.prac_last_name AS prac_lname, u.first_name, u.last_name, u.status, u.created_on, u.updated_at, date_part('month', u.updated_at) AS billing_month, vp.costcode
   FROM app_user u, vwprojects vp
  WHERE vp.id = u.project_id AND u.oldreport = false AND
  date_part('month', u.updated_at) = :month_selected
  AND date_part('year', u.updated_at) = :year_selected
  ORDER BY vp.prac_last_name, u.created_on DESC
]]>

    </sql-query>




</hibernate-mapping>

