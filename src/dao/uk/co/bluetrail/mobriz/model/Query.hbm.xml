<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-mapping PUBLIC
    "-//Hibernate/Hibernate Mapping DTD 3.0//EN" 
    "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>

 
    <sql-query name="findSurveysWithResponseCount">
 		<return alias="survey" class="uk.co.bluetrail.mobriz.model.Survey"/>
	<![CDATA[
	    select s.*, count as responseCount from mr.surveys s, mr.responsecount r where s.id = r.id and s.account_id= :account_id]]>
	</sql-query>


    <sql-query name="findMonthTotals">

        <![CDATA[
	 SELECT (vwtotals.pr_fn || ' ') || vwtotals.pr_ln AS prac_name, sum(vwtotals.num) AS count
   FROM vwtotals
  WHERE date_part('month', vwtotals.updated_at) = :month_selected AND
  date_part('year', vwtotals.updated_at) = :year_selected  and department = :dept_selected
  GROUP BY (vwtotals.pr_fn || ' ') || vwtotals.pr_ln
  ORDER BY (vwtotals.pr_fn || ' ') || vwtotals.pr_ln

]]>

    </sql-query>



    <sql-query name="findTeamReportsMonthTotals">

        <![CDATA[

     SELECT (vp.prac_first_name || ' ') || vp.prac_last_name AS prac_name, count(*) AS count
        FROM app_user u, vwprojects vp, mr.miroteams t
        WHERE u.miroteam_id = t.id and
        vp.id = u.project_id AND u.oldreport = false
        AND date_part('month', u.miroteamadded_on)  = :month_selected
        AND date_part('year', u.miroteamadded_on) = :year_selected
        AND department = :dept_selected
        GROUP BY (vp.prac_first_name || ' ') || vp.prac_last_name
        ORDER BY (vp.prac_first_name || ' ') || vp.prac_last_name


]]>

    </sql-query>




</hibernate-mapping>

